import 'package:flutter/material.dart';

/// The RU Search page (a Rutgers global search)
class RuSearch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("RU Search content here"),
    );
  }
}
